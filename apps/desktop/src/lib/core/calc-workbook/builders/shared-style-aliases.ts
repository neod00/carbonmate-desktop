/**
 * Bold 변형 폰트 (합계/총계 셀에 사용) — 별도 파일로 분리해 빌더 간 공유.
 */

export const HEADER_FONT_BOLD_VARIANT = {
  name: '맑은 고딕',
  size: 10,
  bold: true,
  color: { argb: 'FF000000' },
}

export { applyPrintDefaults } from '../styles'
