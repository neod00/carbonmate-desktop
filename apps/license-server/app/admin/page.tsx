/**
 * Admin 페이지 (Phase 0 placeholder).
 *
 * Phase 2에서 구현:
 * - 비밀번호 입력 (env ADMIN_PASSWORD)
 * - 라이선스 발급 폼 (고객명, 이메일, 만료일)
 * - 발급 시 Neon에 insert + Resend로 이메일 발송
 * - 발급된 라이선스 목록/폐기 버튼
 */
export default function AdminPage() {
  return (
    <main style={{ maxWidth: 720, margin: '64px auto', padding: '0 24px' }}>
      <h1>Admin</h1>
      <p>라이선스 발급 페이지 (Phase 2 구현 예정).</p>
    </main>
  );
}
